{% extends 'core/index.html.twig' %}

{% block studies %}

    <div class="list-group-item disabled bg-orange text-dark font-italic font-weight-bold">
        <h5>OVNIS recensés en {{ studies[2].nomDepartement }} :</h5></div>

    <div class="ul-wrap">
        <div class="list-group list-group-flush">

            {% for el in studies %}

                <a href="{{ path('get_case', { dept: el.departement, id: el.id }) }}" class="list-group-item list-group-item-action ">
                    <p class="badge badge-secondary badge-pill mr-3">{{ el.categoriePAN }}</p>
                    <p class="d-inline-block align-top w-25 mr-2">{{ el.nom }}</p> <p class="d-inline-block w-65 align-top"> {{ el.resumeCourt }}</p>
                </a>

            {% endfor %}

        </div>
    </div>

{% endblock %}


{% block javascripts %}

    <script type="text/javascript">
        //**************
        // See about Jquery

        let id = '{{ id }}';
        let element = document.getElementById("_" + id);
        element.classList.add("sel-1");
    </script>

{% endblock %}

